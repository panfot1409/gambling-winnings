# Pinned uv 0.8.17 installer for CI — official PyPI artifacts, no `curl | sh`.
#
# Provenance: the SHA-256 digests below are the official PyPI artifacts for
# uv 0.8.17, read from https://pypi.org/pypi/uv/0.8.17/json (an independently
# verifiable official source). CI installs uv with
# `pip install --require-hashes --only-binary=:all: -r ci/uv-requirements.txt`,
# so no downloaded byte is piped to a shell and any content mismatch fails the
# install. The wheel hash is used on the x86_64 Linux runners; the sdist hash
# is retained for provenance.
#
#   uv-0.8.17-py3-none-manylinux_2_17_x86_64.manylinux2014_x86_64.whl
#   uv-0.8.17.tar.gz
uv==0.8.17 \
    --hash=sha256:b6d30d02fb65193309fc12a20f9e1a9fab67f469d3e487a254ca1145fd06788f \
    --hash=sha256:2afd4525a53c8ab3a11a5a15093c503d27da67e76257a649b05e4f0bc2ebb5ae
