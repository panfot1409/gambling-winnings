{
  "plan_path": "research/m2b/discovery_plan.json",
  "attempt_id": "discovery-001",
  "note": "Committing/updating this file fires .github/workflows/m2b-acquire.yml on this branch (workflow_dispatch is unavailable because the workflow is not on the default branch). The bot raw-data commit does not touch this file, so it does not retrigger."
}
