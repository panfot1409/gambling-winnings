{
  "attempt_id": "coinbase-btc-usd-research-genesis-001",
  "note": "Transient push-sentinel that bootstraps the one-shot v2b-acquire.yml workflow. Changing this file's attempt_id and pushing to claude/v2b-cross-asset-research-reset triggers exactly one BTC-USD acquisition for that whitelisted attempt. This sentinel and the workflow are removed after the genesis + audit acquisitions are verified (V2B section 13)."
}
