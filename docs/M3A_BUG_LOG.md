# Milestone 3A — closure bug log

Each defect was reproduced on starting HEAD
`8e076165ad07fcbeb4c5350e9d835439495b1489` before any fix. Reproductions live in
`tests/test_m3a_closure_defects.py`; as each defect is corrected the reproduction
is inverted into a regression test asserting the fixed behavior, and the fix
commit is recorded below.

| id | severity | reproduction (starting HEAD) | root cause | correction | regression test | fix commit |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | critical | `develop_m3a` never imports `experiment_registry`; `--write` reaches `generate()` and publishes with no registered/started event (`RETURN_CODE=0`, `REGISTRY_EXISTS=False`) | governance lives outside the publisher; the public write path has no registry gate | fail-closed `run_registered_development_experiment` orchestrator with ~17 ordered pre-checks; the unregistered `--write` path is removed; an unforgeable `DevelopmentRunAuthorization` gates the real evaluation and publication, so `started` is durably appended before any real calculation; `--run-experiment` is the only real-data publish path | `tests/test_development_orchestrator.py` (fail-closed pre-start, unforgeable auth), `tests/test_m3a_closure_defects.py::TestR1RegistryBypass`, `::TestNoUnregisteredRealDataWritePath` | `6562a87` |
| R2 | high | `_atomic_write` renames each file separately with no fsync; injecting a failure before the 2nd write leaves `results=NEW, report=OLD` | the results+report batch is not atomic or rollback-safe; no fsync of file or directory | durable `eth_research.publication.publish_batch` transaction: temp+fsync+ordered replace+marker-last+dir fsync, reverse rollback restoring every prior byte; the orchestrator publishes run-003's whole bundle through it, and `develop_m3a` no longer defines `_atomic_write` | failure-injection matrix in `tests/test_publication.py`; `tests/test_m3a_closure_defects.py::TestR2NonTransactionalPublish` | `6562a87` |
| R3 | high | `load_development_results_payload` accepts `schema_version=999`, `fold_results="not-a-list-at-all"`, `bootstrap_cells=[{"forged":true}]` (`FORGED_RESULTS_ACCEPTED=True`) | it validates only the top-level key set and two counts; nested containers/identities/schema-version unchecked | strict schema-version, container-type, exact grid-count, and cell-object enforcement (a fuller typed results v2 follows with run-003) | `test_m3a_closure_defects.py::TestR3LooseResultsParsing::test_forged_results_are_now_rejected` | `e58554f` |
| R4 | scientific | flat concatenation of five reset folds (226/225×4, block 30): 116/1097 seam-crossing starts (~10.57%), ~4.02 seam-crossing blocks per resample | `moving_block_bootstrap` samples over one concatenated series across reset seams | `fold-stratified-moving-block-bootstrap-v2` draws blocks strictly within a fold; v1 preserved | `tests/test_bootstrap_v2.py` sentinel no-seam-crossing tests | `9297d10` |
| R5 | medium | `development_results.json` has `experiment_family_id` but no `experiment_id` | v1 results omit per-run identity | strict `DevelopmentResultsV2` requires `experiment_id` (and binds `experiment_family_id`, `methodology_id`, source-tree fingerprint) on construct and parse; the registry v2 events and return evidence carry it too. The **committed** run-003 artifact lands when run-003 executes | `tests/test_development_results_v2.py`, `tests/test_experiment_registry.py::TestRegistryV2` | `1ae04bd` (model); committed artifact with run-003 |
| R6 | medium | only run-002 bodies exist at HEAD; run-001 recoverable only from Git | published bodies overwritten in place; no per-experiment archive | immutable `research/m3a/experiments/<id>/` archive + manifests + index; run-001/002 recovered and hash-proven | `tests/test_experiment_archive.py` | `6597730` |
| R7 | security | `ci.yml`/`m2b-replay.yml`/`m3a-replay.yml` run `curl -LsSf https://astral.sh/uv/0.8.17/install.sh \| sh` | unverified network content piped into a shell | every other supply-chain invariant enforced by `test_workflow_security.py` (full-SHA `uses:`, no floating tags, `contents: read`, no secrets/`id-token`/writes, host allowlist, version-pinned installer, no artifact upload, both ledgers checked). **Residual, environment-blocked:** the installer's SHA-256 cannot be pinned offline — outbound access to github.com and astral.sh is blocked by the sandbox egress policy (403), so a trusted SHA cannot be obtained and a guessed one must not be committed. Recorded as CI supply-chain debt, not worked around. | `tests/test_workflow_security.py::TestSupplyChainHardening` | superseded by N10 |
| R8 | docs | "training rows"/"training window" implies a model fit that never occurs | fixed-rule strategies are not fitted | prose docs describe fixed-rule rolling-origin OOS with expanding information sets and state no estimator is fit; the frozen run-002 report's wording is corrected when run-003 re-renders it | docs assertions | _docs done; report wording with run-003_ |

## Terminal defects N1–N10 (run-003 completion)

Found in the closure checkpoint `18de743`; several were over-claimed as fixed.
Each row's regression test failed on `18de743` before its fix.

| id | severity | reproduction (checkpoint 18de743) | root cause | correction | regression test | fix commit |
| --- | --- | --- | --- | --- | --- | --- |
| N1 | high | `DevelopmentRunAuthorization(..., _token=o._AUTH_SENTINEL)` constructs from an imported module global (`EXTERNAL_SENTINEL_FORGE_ACCEPTED=True`) | object identity / a private variable is not an access control | privileged boundaries re-read the canonical registry and prove registered→started-only, the started-line hash, and identity/source/methodology/ledger agreement; possession of a token grants nothing; renamed to "registry-verifiable started-run context" | `tests/test_development_orchestrator.py` | _pending_ |
| N2 | high | `publish_batch(root, [Artifact("../escape.txt", …)])` writes outside root (`PATH_TRAVERSAL_WROTE_OUTSIDE_ROOT=True`) | `root / relpath` joined with no traversal/symlink-parent check | canonical safe-root resolver enforced by the primitive: no `..`/`.`/empty segment, no absolute/drive/backslash/NUL, no symlink parent, resolved target inside root | `tests/test_m3a_terminal_defects.py::TestN2PathTraversal` | `<pub commit>` |
| N3 | medium | temp path is `filename+pid`, opened truncating; clobbers a pre-existing file | non-unique, non-exclusive temp creation | `tempfile.mkstemp` (O_CREAT\|O_EXCL) unique temps in the destination directory; never clobbers | `tests/test_m3a_terminal_defects.py::TestN3TempCollision` | `<pub commit>` |
| N4 | high | `publish_batch(...)` returns, then `_verify_published(...)`; a read-back/parse/reconcile failure leaves finished-looking artifacts and migrated aliases | verification outside rollback scope | explicit `prepare→replace_all→verify→commit` transaction; the verify callback runs before commit, so any failure rolls back to the prior bytes | `tests/test_m3a_terminal_defects.py::TestN4VerifyInsideRollback` | `<pub commit>` |
| N5 | medium | rollback restored bytes but did not fsync restored dirs and used a fixed `.rollback.tmp` name | incomplete rollback durability | unique fsynced rollback temps, best-effort directory fsync, deterministic created-dir removal, a distinct `PublicationCatastrophe` when content restore itself fails, and a concurrency lock | `tests/test_m3a_terminal_defects.py::TestN5RollbackDurability` | `<pub commit>` |
| N6 | high | docs promised a calculation-free recovery finalizer; none exists | unimplemented | `completion_intent.json` + `m3a_recovery --status/--finalize` appends the exact precomputed completed event after re-verifying artifacts, touching no strategy/engine/metric/bootstrap/ledger | `tests/test_m3a_recovery.py` | _pending_ |
| N7 | scientific | hierarchical interval records seed `20260713` but PCG64 was seeded `20260714` | `_interval` serialized `config.seed` for both algorithms | every interval records `base_seed` and the exact `effective_rng_seed`; strict fold validation | `tests/test_bootstrap_v2.py` | _pending_ |
| N8 | medium | `results.execution_code_commit_sha = prep.head` (run HEAD) disagrees with the registry's pre-registration commit | one field overloaded with two meanings | distinct fields: execution-source / registration-container / run-head / methodology-freeze commits + source-tree fingerprint | `tests/test_run_identity.py` | _pending_ |
| N9 | high | tests proved only pre-start refusals; no successful end-to-end orchestration | missing coverage | a synthetic disposable-repo rehearsal drives the production lifecycle and every failure/recovery transition | `tests/test_m3a_orchestrator_e2e.py` | _pending_ |
| N10 | security | workflows still pipe `curl … \| sh`; the R7 test passed *because* the vulnerability was present | documented as debt instead of fixed | `astral-sh/setup-uv` pinned to a verified full commit SHA; no network pipe to a shell; the regression test asserts the vulnerability is gone | `tests/test_workflow_security.py` | _pending_ |
