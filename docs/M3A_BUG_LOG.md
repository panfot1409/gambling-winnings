# Milestone 3A — closure bug log

Each defect was reproduced on starting HEAD
`8e076165ad07fcbeb4c5350e9d835439495b1489` before any fix. Reproductions live in
`tests/test_m3a_closure_defects.py`; as each defect is corrected the reproduction
is inverted into a regression test asserting the fixed behavior, and the fix
commit is recorded below.

| id | severity | reproduction (starting HEAD) | root cause | correction | regression test | fix commit |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | critical | `develop_m3a` never imports `experiment_registry`; `--write` reaches `generate()` and publishes with no registered/started event (`RETURN_CODE=0`, `REGISTRY_EXISTS=False`) | governance lives outside the publisher; the public write path has no registry gate | fail-closed `run_registered_development_experiment` orchestrator with ~17 ordered pre-checks; the unregistered `--write` path is removed; an unforgeable `DevelopmentRunAuthorization` gates the real evaluation and publication, so `started` is durably appended before any real calculation; `--run-experiment` is the only real-data publish path | `tests/test_development_orchestrator.py` (fail-closed pre-start, unforgeable auth), `tests/test_m3a_closure_defects.py::TestR1RegistryBypass`, `::TestNoUnregisteredRealDataWritePath` | `<this commit>` |
| R2 | high | `_atomic_write` renames each file separately with no fsync; injecting a failure before the 2nd write leaves `results=NEW, report=OLD` | the results+report batch is not atomic or rollback-safe; no fsync of file or directory | durable `eth_research.publication.publish_batch` transaction: temp+fsync+ordered replace+marker-last+dir fsync, reverse rollback restoring every prior byte; the orchestrator publishes run-003's whole bundle through it, and `develop_m3a` no longer defines `_atomic_write` | failure-injection matrix in `tests/test_publication.py`; `tests/test_m3a_closure_defects.py::TestR2NonTransactionalPublish` | `<this commit>` |
| R3 | high | `load_development_results_payload` accepts `schema_version=999`, `fold_results="not-a-list-at-all"`, `bootstrap_cells=[{"forged":true}]` (`FORGED_RESULTS_ACCEPTED=True`) | it validates only the top-level key set and two counts; nested containers/identities/schema-version unchecked | strict schema-version, container-type, exact grid-count, and cell-object enforcement (a fuller typed results v2 follows with run-003) | `test_m3a_closure_defects.py::TestR3LooseResultsParsing::test_forged_results_are_now_rejected` | `e58554f` |
| R4 | scientific | flat concatenation of five reset folds (226/225×4, block 30): 116/1097 seam-crossing starts (~10.57%), ~4.02 seam-crossing blocks per resample | `moving_block_bootstrap` samples over one concatenated series across reset seams | `fold-stratified-moving-block-bootstrap-v2` draws blocks strictly within a fold; v1 preserved | `tests/test_bootstrap_v2.py` sentinel no-seam-crossing tests | `9297d10` |
| R5 | medium | `development_results.json` has `experiment_family_id` but no `experiment_id` | v1 results omit per-run identity | results v2 carries `experiment_id` on every artifact/event (delivered with run-003) | `tests/test_results_v2.py::test_results_carry_experiment_id` | _pending (run-003)_ |
| R6 | medium | only run-002 bodies exist at HEAD; run-001 recoverable only from Git | published bodies overwritten in place; no per-experiment archive | immutable `research/m3a/experiments/<id>/` archive + manifests + index; run-001/002 recovered and hash-proven | `tests/test_experiment_archive.py` | `6597730` |
| R7 | security | `ci.yml`/`m2b-replay.yml`/`m3a-replay.yml` run `curl -LsSf https://astral.sh/uv/0.8.17/install.sh \| sh` | unverified network content piped into a shell | every other supply-chain invariant enforced by `test_workflow_security.py` (full-SHA `uses:`, no floating tags, `contents: read`, no secrets/`id-token`/writes, host allowlist, version-pinned installer, no artifact upload, both ledgers checked). **Residual, environment-blocked:** the installer's SHA-256 cannot be pinned offline — outbound access to github.com and astral.sh is blocked by the sandbox egress policy (403), so a trusted SHA cannot be obtained and a guessed one must not be committed. Recorded as CI supply-chain debt, not worked around. | `tests/test_workflow_security.py::TestSupplyChainHardening` | `e58554f`+ (partial; residual documented) |
| R8 | docs | "training rows"/"training window" implies a model fit that never occurs | fixed-rule strategies are not fitted | prose docs describe fixed-rule rolling-origin OOS with expanding information sets and state no estimator is fit; the frozen run-002 report's wording is corrected when run-003 re-renders it | docs assertions | _docs done; report wording with run-003_ |
